# =============================================================================
LAN_IP=192.168.1.248
# ============================================================================

# Host configuration
HOST=0.0.0.0

# Service URLs (used for inter-service communication)
AUTH_SERVICE_URL=http://${LAN_IP}:xxxxx
USER_SERVICE_URL=http://${LAN_IP}:3xxxx
......................... more env variables, configure please as show in the example. just change localhost for lan_ip leave
the rest as it is