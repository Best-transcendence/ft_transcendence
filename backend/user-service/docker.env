# User Service Environment Variables for Docker
# This file contains environment variables for Docker deployment

# Service Configuration
USER_SERVICE_PORT=3002
USER_SERVICE_URL=http://localhost:3002

# Database Configuration (SQLite for School 42 compatibility)
USER_DATABASE_URL=file:./data/user.db

# JWT Configuration (should match auth-service)
JWT_SECRET=super-secret-pass-change-in-production

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3003

# Logging
NODE_ENV=production
LOG_LEVEL=info
